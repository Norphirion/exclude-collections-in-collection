node_modules/
.DS_Store
.env
.env.local
*.log
.vscode/settings.json
.idea/
*.swp

# Build output. PluginDatabase's CI runs `pnpm install && pnpm run build` and
# then packages the .millennium directory it produced, so shipping it here
# would only risk publishing a stale bundle.
.millennium/
